server:
  port: 8082
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  flyway:
    enabled: false
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: none
      use_sql_comments: true
      format_sql: true
